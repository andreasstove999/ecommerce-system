{
  "$id": "https://ecommerce.example/contracts/cart/cart-checked-out",
  "title": "CartCheckedOut Envelope",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "data"],
  "properties": {
    "schema": { "const": "cart/cart-checked-out" },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "eventType",
        "cartId",
        "userId",
        "items",
        "totalAmount",
        "timestamp"
      ],
      "properties": {
        "eventType": { "const": "CartCheckedOut" },
        "cartId": { "type": "string", "minLength": 1 },
        "userId": { "type": "string", "minLength": 1 },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["productId", "quantity", "price"],
            "properties": {
              "productId": { "type": "string", "minLength": 1 },
              "quantity": { "type": "integer", "minimum": 1 },
              "price": { "type": "number", "minimum": 0 }
            }
          }
        },
        "totalAmount": { "type": "number", "minimum": 0 },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    }
  }
}
