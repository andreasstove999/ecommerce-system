{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/contracts/events/payment/PaymentSucceeded.v1.enveloped.schema.json",
  "title": "PaymentSucceeded Event v1 (Enveloped)",
  "allOf": [
    {
      "$ref": "../envelope/EventEnvelope.v1.schema.json"
    },
    {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "eventName": { "const": "PaymentSucceeded" },
        "eventVersion": { "const": 1 },
        "producer": {
          "type": "string",
          "enum": ["payment-service", "payment-service-dotnet"],
          "description": "Payment service emitting success events"
        },
        "partitionKey": {
          "type": "string",
          "description": "Use the orderId to ensure ordering per order",
          "minLength": 1
        },
        "schema": {
          "type": "string",
          "const": "contracts/events/payment/PaymentSucceeded.v1.payload.schema.json"
        },
        "payload": {
          "$ref": "./PaymentSucceeded.v1.payload.schema.json"
        }
      }
    }
  ]
}
